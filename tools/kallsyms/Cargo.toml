[package]
name = "hyper-kallsyms"
version = "0.0.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[dependencies]
rustc-demangle = { version = "0.1.28", default-features = false }

[lints.clippy]
expect_used = "deny"
unwrap_used = "deny"
